import { BadgeIndianRupee, CreditCard, Link, ReceiptIndianRupee, RefreshCcw, WalletCards } from "lucide-react";
import { StatsCard } from "../../../components/ui";
import type { BillingStats } from "../types";
import { rupee } from "../utils";
export function BillingStatsGrid({ stats }: { stats: BillingStats }) { return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4 2xl:grid-cols-8"><StatsCard label="Today Revenue" value={rupee(stats.todayRevenue)} icon={BadgeIndianRupee} /><StatsCard label="Consultation Revenue" value={rupee(stats.consultationRevenue)} icon={ReceiptIndianRupee} tone="blue" /><StatsCard label="Pharmacy Revenue" value={rupee(stats.pharmacyRevenue)} icon={WalletCards} /><StatsCard label="Pending Payments" value={rupee(stats.pendingPayments)} icon={CreditCard} tone="amber" /><StatsCard label="Partial Payments" value={rupee(stats.partialPayments)} icon={CreditCard} tone="blue" /><StatsCard label="Refunds" value={rupee(stats.refunds)} icon={RefreshCcw} tone="violet" /><StatsCard label="Bills Generated" value={String(stats.billsGenerated)} icon={ReceiptIndianRupee} /><StatsCard label="Online Payment Links" value={String(stats.onlinePaymentLinks)} icon={Link} tone="amber" /></div>; }

import { Trash2 } from "lucide-react";
import { Button, Input } from "../../../components/ui";
import type { BillItem } from "../types";
import { rupee } from "../utils";
export function BillItemRow({ item, onChange, onRemove }: { item: BillItem; onChange: (item: BillItem) => void; onRemove: () => void }) { const total = item.quantity * item.unitPrice - item.discount + item.tax; return <div className="grid gap-3 rounded-xl border p-3 md:grid-cols-[1fr_.7fr_90px_110px_110px_90px_90px_auto]"><Input value={item.name} onChange={(e) => onChange({ ...item, name: e.target.value })} placeholder="Item/service name" /><Input value={item.category} onChange={(e) => onChange({ ...item, category: e.target.value })} placeholder="Category" /><Input type="number" value={item.quantity} onChange={(e) => onChange({ ...item, quantity: Number(e.target.value) })} /><Input type="number" value={item.unitPrice} onChange={(e) => onChange({ ...item, unitPrice: Number(e.target.value) })} /><Input type="number" value={item.discount} onChange={(e) => onChange({ ...item, discount: Number(e.target.value) })} /><Input type="number" value={item.tax} onChange={(e) => onChange({ ...item, tax: Number(e.target.value) })} /><div className="grid place-items-center text-sm font-bold">{rupee(total)}</div><Button type="button" variant="ghost" onClick={onRemove}><Trash2 className="size-4" /></Button></div>; }

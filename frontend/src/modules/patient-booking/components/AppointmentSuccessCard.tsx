import { CalendarPlus, CheckCircle2, Download, Printer } from "lucide-react";
import { Link } from "react-router-dom";
import { Button } from "../../../components/ui";
import type { BookingStatusResult, ClinicBookingProfile } from "../types";
export function AppointmentSuccessCard({ result, clinic }: { result: BookingStatusResult; clinic: ClinicBookingProfile }) { return <div className="card mx-auto w-full max-w-xl p-6 text-center"><div className="mx-auto grid size-16 place-items-center rounded-full bg-emerald-100 text-emerald-700"><CheckCircle2 className="size-9" /></div><h1 className="mt-5 text-2xl font-bold">Appointment Confirmed</h1><p className="mt-2 text-sm text-slate-500">Your appointment is confirmed. Please arrive 10 minutes early.</p><div className="mt-5 rounded-2xl bg-slate-50 p-4 text-left text-sm"><p><b>Booking ID:</b> {result.bookingId}</p><p><b>Token:</b> {result.token}</p><p><b>Doctor:</b> {result.doctorName}</p><p><b>Date/time:</b> {result.dateTime}</p><p><b>Patient:</b> {result.patientName}</p><p><b>Clinic:</b> {clinic.address}</p></div><div className="mt-5 grid gap-2 sm:grid-cols-2"><Button variant="secondary">WhatsApp confirmation</Button><Button variant="secondary">Reminder placeholder</Button><Button variant="secondary" icon={<CalendarPlus className="size-4" />}>Add to Calendar</Button><Button variant="secondary" icon={<Printer className="size-4" />}>Print</Button><Button variant="secondary" icon={<Download className="size-4" />}>Download</Button><Link to={`/book/${clinic.slug}`}><Button className="w-full">Back to clinic page</Button></Link></div></div>; }

import { CalendarDays, Clock3, MapPin, Phone } from "lucide-react";
import { Link } from "react-router-dom";
import { BrandLogo, Button } from "../../../components/ui";
import type { ClinicBookingProfile } from "../types";

export function ClinicHero({ clinic }: { clinic: ClinicBookingProfile }) {
  return <section className="overflow-hidden rounded-[2rem] bg-[#0b5964] text-white shadow-card"><div className="grid gap-6 p-6 md:grid-cols-[1fr_.8fr] md:p-8"><div><div className="flex items-center gap-3"><div className="rounded-2xl bg-white p-1"><BrandLogo className="size-16" /></div><div><p className="text-xs font-bold uppercase tracking-[.18em] text-brand-100">Book Your Appointment</p><h1 className="font-['Manrope'] text-3xl font-extrabold">{clinic.name}</h1></div></div><p className="mt-4 text-brand-50">{clinic.specialization}</p><div className="mt-5 grid gap-2 text-sm text-slate-100"><span className="flex gap-2"><MapPin className="size-4" />{clinic.address}</span><span className="flex gap-2"><Clock3 className="size-4" />{clinic.hours}</span><span className="flex gap-2"><Phone className="size-4" />{clinic.phone}</span></div><div className="mt-6 flex flex-wrap gap-2"><Link to={`/book/${clinic.slug}/flow`}><Button icon={<CalendarDays className="size-4" />}>Book Appointment</Button></Link><Button variant="secondary">Call clinic</Button><Button variant="secondary">WhatsApp</Button><Button variant="ghost" className="text-white hover:bg-white/10">Google Maps</Button></div></div><div className="rounded-3xl bg-white/10 p-5"><p className="font-bold">Today availability</p><div className="mt-4 grid gap-3">{["Dr. Priya · 4 slots", "Dental · 2 slots", "Skin · 3 slots"].map((x) => <div key={x} className="rounded-2xl bg-white/10 p-3 text-sm">{x}</div>)}</div></div></div></section>;
}

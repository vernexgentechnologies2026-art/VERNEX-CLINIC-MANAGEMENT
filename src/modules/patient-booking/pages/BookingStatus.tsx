import { useState } from "react";
import { Button, Input } from "../../../components/ui";
import { getBookingStatus } from "../../../services/patientBooking.service";
export default function BookingStatus() { const [searched, setSearched] = useState(false); const result = getBookingStatus(); return <main className="min-h-screen bg-[#f7f9fa] px-4 py-8"><div className="mx-auto max-w-xl"><div className="card p-6"><h1 className="text-2xl font-bold">Check Booking Status</h1><p className="mt-2 text-sm text-slate-500">Enter phone number and booking ID / token number.</p><div className="mt-5 grid gap-3"><Input placeholder="+91 98765 43210" /><Input placeholder="Booking ID or Token number" /><Button onClick={() => setSearched(true)}>Search</Button></div></div>{searched && <div className="card mt-5 p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-wide text-slate-400">Appointment status</p><h2 className="mt-1 text-xl font-bold">{result.status.toUpperCase()}</h2></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{result.token}</span></div><div className="mt-4 space-y-2 text-sm"><p><b>Doctor:</b> {result.doctorName}</p><p><b>Date/time:</b> {result.dateTime}</p><p><b>Clinic:</b> {result.clinicName}</p><p><b>Patient:</b> {result.patientName}</p></div><div className="mt-5 grid gap-2 sm:grid-cols-2"><Button variant="secondary">Reschedule</Button><Button variant="secondary">Cancel</Button><Button>Call clinic</Button><Button>WhatsApp clinic</Button></div></div>}</div></main>; }

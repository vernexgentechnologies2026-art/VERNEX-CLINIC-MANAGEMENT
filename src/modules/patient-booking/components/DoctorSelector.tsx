import { Button } from "../../../components/ui";
import type { BookingDoctor } from "../types";
import { rupee } from "../utils";
export function DoctorSelector({ doctors, selectedId, onSelect }: { doctors: BookingDoctor[]; selectedId: string; onSelect: (id: string) => void }) { return <div className="grid gap-3 md:grid-cols-2">{doctors.map((d) => <button key={d.id} disabled={!d.availableToday} onClick={() => onSelect(d.id)} className={`rounded-3xl border bg-white p-4 text-left transition disabled:opacity-50 ${selectedId === d.id ? "border-brand-500 ring-2 ring-brand-500/20" : "hover:border-brand-300"}`}><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold">{d.name}</h3><p className="text-sm text-slate-500">{d.specialization}</p><p className="mt-1 text-xs text-slate-400">{d.qualification} · {d.experience} yrs</p></div>{d.availableToday && <span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-bold text-emerald-700">Available today</span>}</div><div className="mt-4 flex items-center justify-between"><b>{rupee(d.consultationFee)}</b><Button className="min-h-8 px-3 py-1">{selectedId === d.id ? "Selected" : "Select"}</Button></div></button>)}</div>; }

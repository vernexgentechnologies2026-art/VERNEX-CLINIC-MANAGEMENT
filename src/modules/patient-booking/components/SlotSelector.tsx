import type { AvailableSlot } from "../types";
import { slotTone } from "../utils";
export function SlotSelector({ slots, selectedId, onSelect }: { slots: AvailableSlot[]; selectedId: string; onSelect: (id: string) => void }) { return <div className="space-y-4">{(["morning", "afternoon", "evening"] as const).map((period) => <div key={period}><h3 className="mb-2 text-sm font-bold capitalize">{period} slots</h3><div className="grid grid-cols-2 gap-2 sm:grid-cols-4">{slots.filter((s) => s.period === period).map((s) => <button key={s.id} disabled={s.status === "booked"} onClick={() => onSelect(s.id)} className={`rounded-2xl border px-3 py-3 text-sm font-bold disabled:cursor-not-allowed ${selectedId === s.id ? "border-brand-600 bg-brand-600 text-white" : slotTone[s.status]}`}><span>{s.label}</span><span className="block text-[10px] uppercase">{s.status}</span></button>)}</div></div>)}</div>; }

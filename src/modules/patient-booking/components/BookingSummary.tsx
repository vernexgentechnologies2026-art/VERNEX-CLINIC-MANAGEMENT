import type { BookingDoctor, BookingService, AvailableDate, AvailableSlot, ClinicBookingProfile, PatientBookingInput, PaymentOption } from "../types";
import { paymentLabel, rupee } from "../utils";
export function BookingSummary({ clinic, doctor, service, date, slot, patient, paymentOption }: { clinic: ClinicBookingProfile; doctor: BookingDoctor; service: BookingService; date: AvailableDate; slot: AvailableSlot; patient: PatientBookingInput; paymentOption: PaymentOption }) {
  return <div className="rounded-3xl border bg-white p-5"><h2 className="font-bold">Booking Summary</h2><div className="mt-4 space-y-2 text-sm"><Row label="Clinic" value={clinic.name} /><Row label="Doctor" value={doctor.name} /><Row label="Service" value={service.name} /><Row label="Date" value={date.label} /><Row label="Time" value={slot.label} /><Row label="Patient" value={patient.fullName || "Patient name"} /><Row label="Consultation fee" value={rupee(service.price || doctor.consultationFee)} /><Row label="Payment" value={paymentLabel[paymentOption]} /></div></div>;
}
function Row({ label, value }: { label: string; value: string }) { return <div className="flex justify-between gap-4 border-b border-slate-100 pb-2"><span className="text-slate-500">{label}</span><b className="text-right">{value}</b></div>; }

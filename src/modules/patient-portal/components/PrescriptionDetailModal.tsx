import { Modal, BrandLogo } from "../../../components/ui";
import type { PatientPrescription } from "../types";

export function PrescriptionDetailModal({ prescription, onClose }: { prescription: PatientPrescription | null; onClose: () => void }) {
  return <Modal open={!!prescription} onClose={onClose} title="Prescription">{prescription && <div className="max-h-[72vh] space-y-4 overflow-y-auto pr-1 text-sm"><div className="flex items-center gap-3"><BrandLogo className="size-12" /><div><h3 className="font-bold">Vernex Multispeciality Clinic</h3><p className="text-slate-500">{prescription.date}</p></div></div><div className="rounded-xl bg-slate-50 p-3"><p><b>Doctor:</b> {prescription.doctorName}</p><p><b>Patient:</b> {prescription.patientName}</p><p><b>Diagnosis:</b> {prescription.diagnosis}</p></div><div className="space-y-2">{prescription.medicines.map((item) => <div key={item.medicineName} className="rounded-xl border p-3"><p className="font-bold">{item.medicineName}</p><p>{item.dosage} • {item.frequency} • {item.timing}</p><p className="text-slate-500">{item.duration} • {item.instructions}</p></div>)}</div><div><h4 className="font-bold">Advice</h4><ul className="mt-1 list-disc space-y-1 pl-5">{prescription.advice.map((item) => <li key={item}>{item}</li>)}</ul></div><div><h4 className="font-bold">Lab Tests Suggested</h4><p className="text-slate-500">{prescription.labTests.length ? prescription.labTests.join(", ") : "No lab tests suggested"}</p></div>{prescription.followUpDate && <p className="rounded-xl bg-brand-50 p-3 font-semibold text-brand-700">Follow-up date: {prescription.followUpDate}</p>}</div>}</Modal>;
}

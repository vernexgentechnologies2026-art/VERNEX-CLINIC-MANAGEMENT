import { Button, Input, Modal, Select, Textarea } from "../../../components/ui";
import type { WhatsAppTemplate } from "../types";

export function TemplateEditorModal({ template, onClose }: { template: WhatsAppTemplate | null; onClose: () => void }) {
  return <Modal open={!!template} onClose={onClose} title="Template Editor">{template && <div className="max-h-[72vh] space-y-4 overflow-y-auto pr-1"><Input defaultValue={template.name} placeholder="Template name" /><Select defaultValue={template.category}><option value="booking">Booking</option><option value="reminder">Reminder</option><option value="payment">Payment</option><option value="follow_up">Follow-up</option><option value="review">Review</option><option value="cancellation">Cancellation</option></Select><Textarea rows={6} defaultValue={template.previewText} placeholder="Message body" /><Input defaultValue={template.variables.join(", ")} placeholder="Variables" /><Select defaultValue={template.status}><option value="active">Active</option><option value="draft">Draft</option><option value="needs_api_approval">Needs API approval</option><option value="disabled">Disabled</option></Select><p className="rounded-xl bg-amber-50 p-3 text-xs font-semibold text-amber-700">Final WhatsApp template approval will be handled after official WhatsApp API integration.</p><div className="flex justify-end gap-2"><Button variant="secondary" onClick={onClose}>Cancel</Button><Button onClick={onClose}>Save template</Button></div></div>}</Modal>;
}

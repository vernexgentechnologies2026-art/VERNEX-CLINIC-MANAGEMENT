import { Button, PageHeader } from "../../../components/ui";
import { getMedicineReminderSchedules } from "../../../services/doctor.service";

export default function PatientReminders() {
  return <div className="space-y-5"><PageHeader title="Patient Reminders" description="Doctor-controlled WhatsApp medicine reminders. No patient portal required." /><div className="grid gap-3 lg:grid-cols-2">{getMedicineReminderSchedules().map((reminder) => <div key={reminder.id} className="card p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold">{reminder.patientName}</h3><p className="text-sm text-slate-500">{reminder.phone} - {reminder.prescriptionId}</p></div><span className="rounded-full bg-brand-50 px-2.5 py-1 text-xs font-bold capitalize text-brand-700">{reminder.status}</span></div><p className="mt-3 text-sm"><b>Active medicines:</b> {reminder.activeMedicines.join(", ")}</p><p className="mt-1 text-sm"><b>Next reminder:</b> {reminder.nextReminder}</p><p className="mt-1 text-sm"><b>Duration:</b> {reminder.duration} - <b>Consent:</b> {reminder.consent === "confirmed" ? "Patient consent confirmed" : "Consent not received"}</p><div className="mt-4 flex flex-wrap gap-2"><Button variant="secondary">View schedule</Button><Button variant="secondary">Pause</Button><Button variant="secondary">Resume</Button><Button variant="danger">Stop</Button><Button variant="ghost">Send test preview</Button></div></div>)}</div></div>;
}

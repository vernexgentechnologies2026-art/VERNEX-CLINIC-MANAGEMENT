import { Button, Input, Modal, Select, Textarea } from "../../../components/ui";
import { doctors, services, timeSlots } from "../mock";

export function AppointmentFormModal({ open, onClose }: { open: boolean; onClose: () => void }) {
  return <Modal open={open} onClose={onClose} title="Add Appointment"><div className="max-h-[70vh] space-y-4 overflow-y-auto pr-1"><div className="grid gap-3 sm:grid-cols-2"><label className="text-xs font-bold">Patient name<Input className="mt-1" placeholder="Patient name" /></label><label className="text-xs font-bold">Phone number<Input className="mt-1" placeholder="+91 98765 43210" /></label><label className="text-xs font-bold">Age<Input className="mt-1" type="number" /></label><label className="text-xs font-bold">Gender<Select className="mt-1"><option>female</option><option>male</option><option>other</option></Select></label><label className="text-xs font-bold">Doctor<Select className="mt-1">{doctors.map((d) => <option key={d.id}>{d.name}</option>)}</Select></label><label className="text-xs font-bold">Service<Select className="mt-1">{services.map((s) => <option key={s.id}>{s.name}</option>)}</Select></label><label className="text-xs font-bold">Date<Input className="mt-1" type="date" defaultValue="2026-07-01" /></label><label className="text-xs font-bold">Time slot<Select className="mt-1">{timeSlots.map((t) => <option key={t.id}>{t.label}</option>)}</Select></label><label className="text-xs font-bold">Source<Select className="mt-1"><option>walk_in</option><option>phone_call</option><option>qr_booking</option><option>whatsapp</option><option>website</option></Select></label><label className="text-xs font-bold">Payment status<Select className="mt-1"><option>pending</option><option>paid</option><option>partial</option></Select></label></div><label className="text-xs font-bold">Notes<Textarea className="mt-1" placeholder="Reason, symptoms, or reception notes" /></label><div className="flex justify-end gap-2"><Button variant="secondary" onClick={onClose}>Cancel</Button><Button onClick={onClose}>Create appointment</Button></div></div></Modal>;
}
